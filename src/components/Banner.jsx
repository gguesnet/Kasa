function Banner({ title, img }) {
  return (
    <div className="wrapper">
      <div className="banner">
        <h1>{title}</h1>
        {/* img */}
      </div>
    </div>
  );
}

export default Banner;
